# Branching Workflow

## Branch Purpose

- `main`: stable code.
- `dev`: development work.
- `docs`: documentation updates.

## Development Flow

1. Branch from `dev`.
2. Work in a feature branch.
3. Open a Pull Request back into `dev`.
4. Merge `dev` into `main` when ready for release.
