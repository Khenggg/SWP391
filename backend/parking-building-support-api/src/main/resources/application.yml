server:
  port: ${SERVER_PORT:8081}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
    username: ${DB_USERNAME:postgres.iqzkddymzmfhyqbfrnyu}
    password: ${DB_PASSWORD:Moghicha12@}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 1

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

jwt:
  issuer: ${JWT_ISSUER:ParkingBuilding.CoreApi}
  audience: ${JWT_AUDIENCE:ParkingBuilding.Frontend}
  secret: ${JWT_SECRET:DEVELOPMENT_SECRET_KEY_FOR_LOCAL_TESTING_ONLY_2026_SWP391}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000}

