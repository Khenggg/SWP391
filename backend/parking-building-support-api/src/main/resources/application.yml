spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${DB_USERNAME:postgres.iqzkddymzmfhyqbfrnyu}
    password: ${DB_PASSWORD:Moghicha12@}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

    show-sql: true
jwt:
  issuer: ${JWT_ISSUER:ParkingBuilding.CoreApi}
  audience: ${JWT_AUDIENCE:ParkingBuilding.Frontend}
  secret: ${JWT_SECRET:DEVELOPMENT_SECRET_KEY_FOR_LOCAL_TESTING_ONLY_2026_SWP391}
